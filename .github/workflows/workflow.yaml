name: workflow

on:
  workflow_run:
    workflows: [deploy]
    types:
      - completed

jobs:
  workflow-job:
    if: github.event.workflow_run.conclusion == 'success'

    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Print event information
        run: |
          # Add deployment steps here
          echo "${{ github.workflow_run}}"
