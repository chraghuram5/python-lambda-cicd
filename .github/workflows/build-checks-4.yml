name: build-checks-4

on: 
  workflow_dispatch:
  push:
    branches: [ test-5 ]

jobs:
  tests:
    name: Format and tests
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    steps:
      - name: Test
        run: echo "Test step"
