name: upload-tests

on:
  workflow_dispatch:
  workflow_run:
    workflows: [ build-checks-1 ]
    types:
      - completed

jobs:
  run-tests:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    name: Upload tests
    runs-on: ubuntu-latest
    steps:
      - name: Run upload-tests
        run: echo "Run upload-tests"
